version: '3.7'

services:

  postgres:
    image: postgres:11
    ports:
      - 5432:5432
    environment:
      - "POSTGRES_PASSWORD=password"

  postgres-test:
    image: postgres:11
    ports:
      - 5433:5432
    environment:
      - "POSTGRES_PASSWORD=password"